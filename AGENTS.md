# Repository instructions

本文件约束整个仓库；子目录 `AGENTS.md` 补充各自任务。冲突时以更深目录的要求为准，但不得降低本文件的证据和安全要求。

## 目标与边界

1. 先让 `native-binary`、`term-transport`、`runtime-nbe` 各自独立通过测试。
2. 路径之间禁止源码依赖、隐式共享构建产物或共享可变状态。
3. `dispatcher` 最后通过公开接口做最小耦合集成；路径未验收前不得用分流器掩盖其缺口。
4. 进度唯一来源是 `ACCEPTANCE.md`。没有代码、可重复测试和对应提交证据，不得勾选。

## 角色

### 本机实现者

- 可以编辑、测试和按用户授权提交。
- 每次只收敛一个明确验收项；先跑该目录独立测试，再跑受影响的集成测试。
- 不提交二进制、缓存、临时目录或只在本机成立的历史证据。
- 本次骨架可直接提交 `main`；后续写入仍需用户明确授权。

### Web 核验者

- 只读，不编辑、不提交、不推送、不新建分支或 PR。
- 必须 fresh clone 指定 commit SHA，确认 `HEAD` 后执行真实命令。
- 不采信 README、自报日志、清单勾选或仅有 CI 绿灯；要检查实现是否进入真实数据流，并运行正例、反例。
- 无法执行时报告 `NOT-RUN`，发现不一致时报 `FAIL`；不得用 `PASS` 代替缺失证据。

## 跨环境规则

- 禁止写死 macOS/Linux 版本、Homebrew/Nix/apt 路径、用户名、工作区绝对路径和固定临时目录。
- 可执行文件由命令行参数、配置或环境变量指定；默认值只能来自 `PATH`/能力探测。
- 以能力探测决定行为，不以操作系统版本字符串猜测能力。
- 版本兼容范围写在配置和文档中；测试记录实际工具版本。影响语义的依赖必须锁定源码 revision 和许可证证据。
- 临时文件、动态库定位、路径分隔和清理由平台 API 或可覆盖配置处理。
- 缺少依赖时应给出稳定、可理解的错误，不能偷偷切换到 mock 或旧产物。

## 测试与证据

- 每个子项目在称为“实现”前，必须提供一个无需其他路径即可运行的测试入口。
- 每项至少包含真实正例、fail-closed 反例、无陈旧产物检查。
- 测试须输出 commit、输入身份、依赖身份和实际工具版本；生成物放入忽略目录。
- 差分测试必须比较同一输入的真实两端结果；相关性测试、手写预期和证明关联不能冒充差分。
- 完成项需同时满足：代码存在、测试实际覆盖、当前提交复现、独立核验通过。

## 提交纪律

- 提交保持单一目的，提交前检查工作树、差异和测试结果。
- 不改写已发布历史，不使用强推；旧版本应可由父提交恢复。
- Web 核验报告必须写明 SHA、环境、执行命令、退出码以及 `PASS/FAIL/NOT-RUN`。

