# Codex Haskell 重跑：持久化通道探针

日期：2026-07-25

## 正式任务目标

找到 Codex CLI 负责编排调度的闭包，用 Haskell 重写；暂时禁用原版 Rust 编排内核；不改变任何功能。

边界：

- Haskell 拥有 turn、state、sampling、tool admission、history 等编排决策。
- Rust 仅保留网络、沙箱、进程、终端等执行适配层。
- 交付修改后的可运行 Codex CLI、源码、测试证据和中文验收报告。

## 本轮范围

本轮只测试持久化通道，不开始源码恢复、修改、编译或测试。

## 后续硬检查点

每到以下节点，立即冻结并持久保存：

1. 原始源码、基线版本和任务清单；
2. 首个生产源码差异；
3. Haskell sidecar 首次可编译版本；
4. Rust 与 Haskell 协议首次贯通版本；
5. core 首次构建通过版本；
6. 每轮完整测试结果；
7. release 构建、staged CLI、源码包、校验和与验收报告。

GitHub 保存采用专用分支和小步提交；大型二进制、日志与归档保存到文件库。

状态：持久化探针；正式重跑尚未开始。
